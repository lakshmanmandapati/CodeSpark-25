# This file was created by mistake - this is a Node.js project
# Dependencies are managed via package.json, not requirements.txt
# Please delete this file or rename it to avoid Vercel confusion

# For Node.js projects, use:
# - package.json for dependencies
# - npm install to install packages
# - npm run build for production builds

# This project structure:
# Frontend: React + Vite (deployed on Vercel)
# Backend: Node.js + Express (deployed on Render)
